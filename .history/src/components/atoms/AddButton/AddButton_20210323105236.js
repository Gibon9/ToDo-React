import React from 'react';
import { styled } from 'styled-components';

const StyledButton = styled.button`
  height: 59px;
  width: 414px;
`;

const AddButton = () => <button>Add ToDo</button>;

export default AddButton;
