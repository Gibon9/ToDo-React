import React from 'react';
import { Label } from 'components/atoms/Label/Label';

const AddForm = ({ name, id, type = 'text', ...props }) => {
  return(
  <>
    <Label htmlFor></Label>
  </>;)
};

export default AddForm;
